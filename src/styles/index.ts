import styled from 'styled-components';

const Div = styled.div`
  height: 100%;
  overflow: hidden;
  position: relative;
  width: 100%;

  > div {
    height: 100%;
    position: absolute;
    width: 100%;
  }
`;

export default Div;
